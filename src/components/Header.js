// IMPORTS
// - libraries
import React, { Component } from 'react';
// - components
import Navigation from './Navigation';

const Header = () => (
  <header>
    <h1>Expensify</h1>
    <Navigation />
  </header>
);

export default Header;
